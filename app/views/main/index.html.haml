#content
  #referrallink
    = link_to t('titles.referral'), '', 'data-toggle' => 'modal', 'data-target' => '#referral'
  = render :partial => 'referral'
  #area
    #rates
      %i.icon-envelope
      #rate
        = @email_clicked_through_rate.to_s + '%'
      %i.icon-comment-alt
      #rate
        = @sms_clicked_through_rate.to_s + '%'